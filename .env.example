# Example environment variables for costwise-planner

# Google Gemini API Key
# Replace 'your_key_here' with your actual key, then rename to .env (ensure .env is in .gitignore)
VITE_GOOGLE_AI_API_KEY=your_key_here